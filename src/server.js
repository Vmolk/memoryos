// server.js — Express server (entry point: `npm start`).
// Phase C will implement: serve public/ (no CDN, vendored assets), and endpoints:
//   POST /api/capture  (text or audio upload) -> transcribe? -> organize -> store -> return memory
//   GET  /api/memories (timeline data + emotion series)
//   POST /api/ask      (v2, after Gate 3) -> semantic search -> RAG answer
// STUB — no logic yet.
