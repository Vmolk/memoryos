// db.js — Local SQLite memory store.
// Phase B will implement schema:
//   memories(id, raw_text, summary, emotion, emotion_score REAL, tags TEXT(JSON),
//            source_type TEXT('voice'|'text'), created_at TEXT(ISO))
// plus insert/list/query helpers. v2 (after Gate 3) adds an embedding BLOB column +
// sqlite vector index. STUB — no logic yet.
